{
  "name": "messages",
  "version": "1.0.0",
  "description": "protobuf definitions of the messages exchanged between processes and over the wire in VU ASE",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ts-proto": "^1.167.0"
  }
}
